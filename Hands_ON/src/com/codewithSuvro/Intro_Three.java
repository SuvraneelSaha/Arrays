package com.codewithSuvro;

public class Intro_Three {
    public static void main(String[] args) {

        int[] arr = new int[5];



        //left side compile time || right side runtime

        // for remembering -- > left ---> compile time && right -->runtime
        // as r stands in both right and runtime


        // the actual object creating in the heap happens at runtime
        // --- > this is known as dynamic memory allocation
         // ie at the runtime the memory is allocated
        
    }
}
